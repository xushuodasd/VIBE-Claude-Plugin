# VIBE-Claude-Plugin 快速使用手册

欢迎使用 VIBE-Claude-Plugin。这是一套为 Claude Code 打造的“24小时全自动开发引擎”，通过模拟“研发总监+员工”的多智能体架构，实现企业级的无人值守编程。

---

## 1. 快速安装
由于 Claude Code 的安全机制，本项目必须按**标准插件**安装。只有当整个插件被完整安装到 Claude Code 的插件目录后，`/vibe` 才会出现。

### 官方安装方式
**第一步**：打开配置文件 `C:\Users\你的用户名\.claude\settings.json`，在里面加上我们插件的源：
```json
{
  "extraKnownMarketplaces": {
    "vibe": {
      "source": {
        "source": "github",
        "repo": "xushuodasd/VIBE-Claude-Plugin"
      }
    }
  }
}
```

**第二步**：重启 Claude Code，在聊天框直接输入：
```bash
/plugin install vibe-claude-plugin@vibe
```

---

### 备用安装方式 (Git 克隆)
如果上面的方法报错，你可以直接把**整个插件仓库**拉取到 Claude Code 的插件目录里。不能只复制 `skills/`，否则 `/vibe` 不会出现。

**Windows (PowerShell) 一键安装:**
```powershell
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.claude\plugins" | Out-Null
git clone https://github.com/xushuodasd/VIBE-Claude-Plugin.git "$env:USERPROFILE\.claude\plugins\vibe-claude-plugin"
```

**macOS / Linux 一键安装:**
```bash
git clone https://github.com/xushuodasd/VIBE-Claude-Plugin.git ~/.claude/plugins/vibe-claude-plugin
```
安装完成后，**彻底重启 Claude Code**（按 Ctrl+C 退出后重新输入 `claude` 启动）。
重启后，先执行：

```bash
/plugins
```

如果列表中能看到 `vibe-claude-plugin`，再执行 `/vibe`。

---

## 2. 核心功能：如何开启 24 小时全自动开发？

为了让 AI 能够不间断干活而**不会停下来向你索要 `y/N` 权限**，请务必按照以下步骤操作：

### 步骤 A：赋予终端执行权限（关键）
在 Claude Code 刚启动时，建议使用 `--trust` 参数，或者在它第一次提示要不要执行终端命令时，选择 **`Yes, don't ask again for this session`**。

### 步骤 B：使用“专属指令”触发引擎
在聊天框中，直接使用我们的专属斜杠命令 `/vibe` 来开启 24 小时全自动开发。

这里要区分两个名字：

- `/vibe`：你在 Claude Code 里输入的**公开入口命令**
- `vibe-autopilot`：被 `/vibe` 调起的**内部自动驾驶引擎**

也就是说，对外使用时请输 `/vibe`，不要把技能名和命令名混在一起。

**标准用法示例：**
```bash
/vibe 我要做一个前端待办事项网站。要求深色工业风，按钮有物理反馈感。后端用 Node.js 存 SQLite。
```

如果你输入 `/vibe` 提示找不到命令，优先检查 3 件事：
1. 你是不是只装了 `skills`，没有装整个插件目录。
2. 你是不是没有彻底重启 Claude Code。
3. 你执行 `/plugins` 后，列表里是否真的出现了 `vibe-claude-plugin`。

---

## 3. 全自动开发期间会发生什么？

引擎启动后，分为两个明确的阶段：

### 阶段一：需求对齐（它会提问）
在这个阶段，AI 扮演**产品经理与架构师**。
- 它会根据你的话术生成 PRD（需求文档）、API 接口文档和架构设计。
- **注意**：此时它可能会向你提问以确认细节（例如：“主题色你想偏蓝还是偏绿？”）。
- 当一切敲定后，它会生成一份 `tasks.md` 任务清单，并问你：“是否可以开始全自动开发？”
- 你回复：**“可以，开始吧”**。

### 阶段二：无人值守开发（它将彻底闭嘴）
一旦你同意开始，AI 将化身为**研发总监和底层打工人**的结合体。
- **彻底静默**：它不会再问你任何问题。遇到任何代码报错，它会自己寻找最优解修复。
- **工作流循环**：它会自动切分支 -> 查 API 文档 -> 写一个最小模块代码 -> 跑测试 -> 查安全漏洞 -> 加 UI 动效 -> `git commit` 提交 -> 在 `tasks.md` 打勾 -> 做下一个任务。
- 你可以去喝杯咖啡，直到它最终把交付报告打印在屏幕上。

---

## 4. 常见问题 (FAQ)

**Q: 如果中途我想打断它或者修改需求怎么办？**
A: 直接在终端按 `Ctrl+C` 强行打断它。跟它说你的新想法，它会重新更新 `tasks.md`，然后你再让它继续。

**Q: 跑了很久以后，感觉它脑子变慢了或者开始胡言乱语怎么办？**
A: 这是大模型的“上下文溢出”现象。直接输入 `/clear` 清空聊天记录。然后对它说：**“读取 tasks.md，继续全自动开发”**。它会瞬间满血复活，找回研发总监的状态继续干活。

**Q: 它可以接手我现有的半成品项目吗？**
A: 可以。直接输入 `/vibe 分析当前项目，并帮我增加一个 XXX 功能`，它会自动去读你现在的代码并生成相应的计划。

---

## 5. 对外文档怎么阅读

如果你是第一次接触这个项目，建议按下面顺序阅读：

1. 先看 [README.md](./README.md) 或 [README.zh-CN.md](./README.zh-CN.md)，了解项目定位与安装方式。
2. 再看本手册，理解实际使用时的步骤和常见问题。
3. 如果你要二次开发或维护插件，再看 `AI_DEVELOPMENT_DOC.md`。
